#Two sum

def two_sum(list1,target):
    for numbers in range(len(list1)):
        for numbers2 in range(len(list1)):
            if list1[numbers]+ list1[numbers2] == target:
                print(numbers, numbers2);
just_list = [1,2,3,4,6,1,2,4];
#target = 4
#two_sum(just_list,target);

def add_two_list(list1,list2):
    list3=[0];
    for numbers in range (len(list1):
        for numbers2 in range(len(list2)):
    sum= ((int)numbers+(int)numbers2)
    if sum<10:
        list3.append(sum);
    else:
        sum = sum -10;
        list3.append(sum);
add_two_numbers()